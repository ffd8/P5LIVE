{
	"presets": [
		{
			"name":"mouseX-mouseY",
			"code":"// shared code for COCODING editor\n/*\nlet x, y;\nfunction parseData(obj){\n\tx = obj.x;\n\ty = obj.y;\n\t\n\t// on-demand or ideally used in draw()\n\tbackground(0);\n\tellipse(x, y, 50);\n}\n*/\n\n\n// preset: mouseX/mouseY \nlet frequency = .05; // # = X per second\n\nsetInterval(function() {\n\tsendPing();\n}, frequency * 1000);\n\nfunction sendPing() {\n\tlet obj = {\n\t\tx: mouseX,\n\t\ty: mouseY,\n\t}\n\tsyncData(obj); // transmit to others\n};"
		},
		{
			"name":"midi",
			"code":""
		}
	]
}

